/*const connectDB = require("./utils/lib/connectDb");
const User = require("./models/students");

async function main() {
  await connectDB();
  const user = await User.findOne({ rollNo: 25 });
  console.log(user);

  if (!user) {
    
  }
}

main();*/